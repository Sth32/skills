<!--
启动门禁：创建或继续本文档前，必须先读取当前工作单元实际依赖的 `01` 至 `04` 阶段文档顶部状态。未分支时正常要求 `01=整理完成`、`02=已收敛`、`03=已收敛`、`04=已完成`；存在 `00-工作流索引.md` 时，必须先解析当前 `Uxx` 的共享上游和直接前驱，只检查真实依赖，不以未依赖的兄弟工作单元阻塞。
阶段完成门禁：只有满足本阶段完成条件、把顶部状态写为 `骨架已完成`、同步最后更新时间，并重新读取确认写入成功后，才能宣布当前工作单元本阶段结束。`骨架代码已落地，待运行验证` 仍是非终态。若当前文档继续拆分，顶部状态写为 `已拆分`，父文档不再直接进入下一阶段，只允许索引中的叶子后继推进。
-->

# <功能名>：框架实现方案

> 状态：方案中 / 待确认 / 骨架代码已落地，待运行验证 / 骨架已完成 / 已拆分
> 工作单元：未分支 / <Uxx>
> 工作流索引：无 / [`00-工作流索引.md`](./00-工作流索引.md)
> 基线提交：
> 最后更新：

## 1. 框架结论

- 功能归属：
- 本阶段建立：<属性、持久化、镜像、客户端视图、空 RPC/事件>
- 明确不实现：<资格、扣除、奖励、状态转换等产品规则>
- 配置依赖接管：<已接管 / 移交完整实现>

## 2. 实体与组件

| 实体/组件 | 所在线程或进程 | 本阶段责任 | 新增结构或接口 |
|---|---|---|---|

## 3. 数据权威与持久化

| 数据/属性 | 权威实体 | 是否存盘/位置 | 镜像持有者 | 起服来源与 ready | 运行时同步 | 客户端可见 |
|---|---|---|---|---|---|---|

补充说明：

- 镜像未就绪行为：
- 缓存或派生数据的重建方式：
- 是否存在特殊双写；若有，一致性责任：

## 4. 客户端可见性

| 数据 | 可见对象 | 权威来源 | 展示镜像/属性 | 全量/增量通道 | 未就绪表现 |
|---|---|---|---|---|---|

## 5. 触发点与消息流转

| 触发点 | 当前实体/线程 | 当前可用数据 | 缺失数据与权威者 | 获取/跨线程方式 | 下一实体 | 技术失败行为 |
|---|---|---|---|---|---|---|

## 6. RPC / 事件骨架

| 接口/事件 | 发起者 | 接收权威实体 | 请求/回包 | 本阶段动作 | 最终空实现 |
|---|---|---|---|---|---|
|  |  |  |  | 解析、寻址、转发 | 副作用前返回未实现 |

## 7. 配置依赖接管

| 配置对象 | 代码路径或符号 | 依赖类型 | 本阶段动作 | 后续阶段 | 状态 |
|---|---|---|---|---|---|
|  |  | 属性/注册/视图/业务规则 |  | game-scaffold / game-implement |  |

## 8. 移交完整实现的业务规则

| 需求规则 | 最终权威实体 | 所需数据 | 预期副作用 | 后续验证 |
|---|---|---|---|---|

> 本节必须覆盖权限、资格、消耗、奖励、状态转换、路径选择和最终结果判断。框架阶段不实现这些内容。

## 9. 用户确认

- 确认结论：
- 修订项：

## 10. 实际骨架与偏差

- 已建立的数据结构：
- 已建立的持久化与启动恢复：
- 已建立的镜像与客户端同步：
- 已建立的 RPC/事件路由：
- 与方案偏差：
- 待 `game-implement`：

## 11. 验证

- Schema/序列化：
- 权威恢复：
- 镜像全量、增量与 ready：
- 客户端视图：
- RPC/事件跨线程路由：
- 目标缺失/超时/未就绪：
- 无业务副作用：
- 启动与既有链路：

<!-- 工作单元与依赖只在 00 中维护；本文档不复制兄弟分支状态。 -->
