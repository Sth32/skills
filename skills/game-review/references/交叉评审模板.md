<!--
启动门禁：创建或继续本文档前，必须先判断是否存在 `00-工作流索引.md`。未分支时按原单线门禁读取 `01` 至 `06`，要求 `01=整理完成`、`02=已收敛`、`03=已收敛`、`04=已完成`、`05=骨架已完成`、`06=已完成`。存在索引时，必须读取完整 DAG 和所有当前叶工作单元，只检查它们实际需要的 pre-review 链路；所有叶工作单元均达到可 review 终态后才能开始 07。被标记 `已拆分` 的父工作单元不是叶节点，也不以父文档的状态替代子工作单元检查。
Review 汇合门禁：`07` 永远只有一份 `07-交叉评审.md`，禁止创建 `07-Uxx-*`。任一叶工作单元尚未闭环时必须停止并指出具体 `Uxx` 与当前文档。全部满足后先把 `00` 的 Review 汇合指向本文件，再开始完整 review。
阶段完成门禁：最终结论必须先写回本文档顶部 `状态` 并同步最后更新时间，再重新读取确认写入成功，之后才能向用户宣布 review 已通过、有条件通过或不通过。聊天结论不能替代顶部状态。
-->

# <功能名>：交叉评审

> 状态：评审中 / 通过 / 有条件通过 / 不通过
> 工作流：未分支 / [`00-工作流索引.md`](./00-工作流索引.md)（已汇合）
> 基线：<base>..<head>
> 文档版本：
> 最后更新：

## 1. Review 范围

- 代码：
- 配置与导表：
- 需求与方案：<未分支链路 / 全部 Uxx + 共享基线>
- 客户端对接：
- 排除项：
- 验证限制：

## 2. Reviewer 分工

| Reviewer | 独立视角 | 使用方法 | 状态 |
|---|---|---|---|

## 3. 已确认发现

| ID | 工作单元 | 级别 | 根因与触发场景 | 证据位置 | 影响 | 处理结论 | 复审 |
|---|---|---|---|---|---|---|---|

## 4. 被证伪或合并的候选

| 候选 | 工作单元 | 原判断 | 证伪/合并证据 | 结论 |
|---|---|---|---|---|

## 5. 文档与测试问题

| ID | 工作单元 | 类型 | 问题 | 影响 | 处理 |
|---|---|---|---|---|---|

## 6. 验证证据

- 构建/启动：
- 关键测试：
- 配置/迁移：
- 故障与回滚：

## 7. 最终结论

- 状态：
- 未解决 S0/S1：
- 接受的条件或风险：
- 建议下一步：

<!--
工作单元列用于把发现路由回正确的 pre-review 文档，不意味着 review 再次分支。
若 review 发现必须新增工作单元，保持本文档为阻塞/不通过，暂停 review，回到 02–06 更新 00；新增工作单元闭环后重新汇合到本文件，不新建第二份 07。
-->
