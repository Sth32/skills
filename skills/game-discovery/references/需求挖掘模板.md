<!--
启动门禁：创建或继续本文档前，必须先读取 `01-原始需求.md` 顶部状态。只有 `状态：整理完成` 才能按正常流程进入本阶段；若仍为 `存在待确认原文` 或状态缺失/异常，必须先明确提醒用户并返回上游闭环，不得仅凭聊天中的“已完成”继续。若存在 `00-工作流索引.md`，还必须先从索引解析当前 `Uxx`、实际依赖和当前文档；未依赖的兄弟工作单元不构成阻塞。
阶段完成门禁：只有满足本阶段收敛条件、把顶部状态写为 `已收敛`、同步未解决计数与最后更新时间，并重新读取确认写入成功后，才能宣布当前工作单元本阶段结束。若当前文档被继续拆分，顶部状态写为 `已拆分`，后续只允许索引中的叶子后继继续推进；父文档不得直接进入下一阶段。聊天结论不能替代顶部状态。
-->

# <功能名>：需求挖掘

> 状态：进行中 / 已收敛 / 已拆分 / 被阻塞  
> 工作单元：未分支 / <Uxx>  
> 工作流索引：无 / [`00-工作流索引.md`](./00-工作流索引.md)  
> 原始需求：[`01-原始需求.md`](./01-原始需求.md)  
> 未解决：P0 <N> / P1 <N> / P2 <N>  
> 最后更新：<YYYY-MM-DD>

## 待确认事项

### Q-001 · P0

**问题**  
<用产品语言提出一个可直接回答的问题>

**必要依据**  
<只保留理解该问题所需的需求事实、现有行为或影响；一到三条>

**建议（可选）**  
<仅在存在明显低风险默认值时填写>

### Q-002 · P1

<下一个独立问题；没有则删除>

<!--
一个问题只保留一个连续分块，问题、依据和建议必须相邻。
不得再创建“已确认结论”“当前批次”“问题台账”“暂不追问”“收敛检查”等分块。

收到回答后必须在同一轮：
1. 先原位更新当前 02 文档，只保留当前未决内容；
2. 部分解决时只保留剩余问题；
3. 删除已经解决、取消、失效或不再有价值的问题分块；
4. 若答案形成正式产品规则，在原问题位置保留最小“待确认回写 01”状态并请求用户确认；
5. 用户确认后才回写 01，再从当前 02 删除临时待回写内容。

仅当确有内容时可追加：

## 风险与延期
- `R-001` <需程序评估的重大实现风险，或负责人明确延期的产品决定>

没有待确认事项或风险时不要创建空章节，也不要写“无”。
文档只表示当前状态，不保存沟通历史。
工作单元与依赖只在 00 中维护；本文档只写本工作单元的产品问题。
-->
