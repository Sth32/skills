# <功能名>：客户端对接文档

> 状态：草稿 / 待确认 / 已确认
> Server 基线：
> 协议/属性定义版本：
> 最后更新：

## 1. 对接范围

- 涉及实体：
- 新增/变化内容：
- 客户端可完成的表现：
- 当前阻塞或待确认：无 / <事项>

## 2. 属性同步

> 一行对应一个属性或紧密相关的属性组，只记录本功能新增、修改或必须依赖的内容。

| 实体/唯一标识 | 属性/类型 | 含义与单位 | 权威者 | 同步实体/组件 | 首次同步 | 增量触发 | 可见范围 | 清除/重同步 |
|---|---|---|---|---|---|---|---|---|

补充语义：

- 枚举与状态：
- 空值/缺省值：
- 时间戳、时区或倒计时基准：
- 配置/资源 ID 的对应关系：

## 3. 实体生命周期与同步变化

| 场景 | Server 行为 | Client 收到的数据 | 客户端可据此判断 |
|---|---|---|---|
| 首次登录/重连 |  |  |  |
| 实体创建/进入可见范围 |  |  |  |
| 属性变化 |  |  |  |
| 实体销毁/离开可见范围 |  |  |  |
| 权限或阵营关系变化 |  |  |  |

删除不涉及的行，不保留空场景。

## 4. RPC 与事件

### 4.1 `<RPC/事件名称>`

- 方向：C2S / S2C 回包 / S2C 推送 / 广播
- 发送者 → 接收者：
- 发送/触发时机：
- 同步回包：无 / <内容>
- 最终结果：同步回包 / `<异步推送>` / 仅属性变化
- 失败与错误语义：

请求或事件参数：

| 参数 | 类型 | 必填 | 含义 | 单位/枚举/限制 |
|---|---|---:|---|---|

回包或异步结果参数：

| 参数 | 类型 | 含义 | 备注 |
|---|---|---|---|

复杂结构确有必要时提供一个最小示例；简单接口不要粘贴生成代码。

## 5. 服务端主动同步/推送汇总

| 推送/属性变化 | 触发条件 | 接收范围 | 关键字段 | 与 RPC 的关系 |
|---|---|---|---|---|

没有独立推送、仅靠属性同步时直接写明。

## 6. 客户端数据充分性

- [ ] 能识别实体并创建表现
- [ ] 能根据属性变化更新状态
- [ ] 能在销毁、不可见或重连时正确移除/恢复
- [ ] 能正确发起 RPC 并关联回包或异步结果
- [ ] 能区分成功、失败、空数据、未开放和同步未完成
- [ ] 能通过配置/资源 ID 获得展示所需资源

## 7. 服务端缺口与待确认

| 优先级 | 缺少的数据或合同 | 对客户端表现的影响 | 建议补齐位置 | 状态 |
|---|---|---|---|---|

没有缺口时写“无”。不要用客户端猜测或本地计算代替服务端合同。
