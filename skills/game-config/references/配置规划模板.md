<!--
启动门禁：创建或继续本文档前，必须先读取当前工作单元实际依赖的 `01` 至 `03` 文档顶部状态。未分支时正常要求 `01=整理完成`、`02=已收敛`、`03=已收敛`；存在 `00-工作流索引.md` 时，必须先解析当前 `Uxx` 的共享上游和直接前驱，只检查真实依赖，不以未依赖的兄弟工作单元阻塞。任一实际依赖状态缺失、异常、仍在进行或被阻塞时，必须明确提醒用户并返回对应上游闭环。
阶段完成门禁：只有满足本阶段完成条件、把顶部状态写为 `已完成`、同步未解决计数与最后更新时间，并重新读取确认写入成功后，才能宣布当前工作单元本阶段结束。若当前文档继续拆分，顶部状态写为 `已拆分`，父文档不再直接进入下一阶段，只允许索引中的叶子后继推进。
顶部“状态”只表示当前工作单元的配置阶段是否闭环；“后续代码依赖”必须留在对应配置事项内，不得用 `待代码接入` 作为阶段顶部状态。
-->

# <功能名>：配置规划

> 状态：规划中 / 待确认 / 修改中 / 待人工 / 已完成 / 已拆分  
> 工作单元：未分支 / <Uxx>  
> 工作流索引：无 / [`00-工作流索引.md`](./00-工作流索引.md)  
> Excel：手动 / 自动 / 不涉及  
> 未解决：P0 <N> / P1 <N> / P2 <N>  
> 最后更新：<日期或提交>

## 配置事项

### C-001 · <逻辑配置对象> · <状态>

- **真实源 / Schema**：<文件、Sheet、字段或声明式 Schema；不列生成物>
- **关键变化**：<参照对象 + 变化字段，不复制完整行或表结构>
- **执行方式**：<Agent 修改 / 人工填表 / 待确认>
- **当前结果**：<已修改内容，或仍需完成的最小操作>
- **验证**：<实际执行的解析、类型、引用、导表、输出或加载检查及结果>
- **后续代码依赖**：<缺少的行为、代码入口和后续阶段；没有则删除本行>

### C-002 · <下一个独立配置对象> · <状态>

<没有则删除>

<!--
每个逻辑配置对象只出现一次。真实源、变化、人工步骤、执行状态、验证结果和代码依赖必须相邻。
不得另建“配置执行清单”“人工填表清单”“验证报告”“调查记录”或“移交摘要”。
用户选择人工填表时，直接在对应事项的“当前结果”中写清 Sheet、定位方式、字段和校验方式。
执行或沟通完成后原位更新：删除已解决问题、旧步骤、旧状态和失效说明，只保留当前真相。
后续代码依赖属于移交信息：当前工作单元配置阶段自身已完成时，顶部状态应写 `已完成`，而不是 `待代码接入`。
工作单元与依赖只在 00 中维护，不在本文档复制兄弟分支状态。
完整 diff、生成日志和执行历史保留在仓库或工具输出中，不复制到本文档。
没有配置事项时不要创建空章节，也不要写“无”。
-->
