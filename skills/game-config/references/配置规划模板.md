# <功能名>：配置规划

> 状态：规划中 / 待确认范围 / 修改中 / 配置已完成待接线 / 已完成
> Excel：待确认 / 手动 / 自动 / 不涉及
> 最后更新：<日期或提交>

## 1. 配置结论

- 逻辑配置对象：<数量>
- 变更层级：数据 / Schema / 混合
- 新增表、Schema 或输出范围：否 / 是（只写原因）
- 当前阻塞：<P0/P1；没有则写“无”>
- 后续代码依赖：<数量；没有则写“无”>

## 2. 逻辑配置对象与执行状态

> 一行对应一个逻辑配置对象。真实源可包含多个文件或 Sheet；只写受影响管线和关键差异。

| 逻辑配置对象 | 变更层级 | 真实源 / Schema | 关键差异 | 输出目标 / 关键消费端 | 执行方式 / 状态 |
|---|---|---|---|---|---|
|  | 数据 / Schema / 混合 |  |  | 客户端 / 服务端 / 工具 / 单一目标 | 待确认 / 直接修改 / Excel 手动 / Excel 自动 / 待代码接入 / 已完成 |

## 3. 需要确认

| 优先级 | 逻辑对象 | 需要决定的事项 | 推荐或默认处理 | 负责人 / 状态 |
|---|---|---|---|---|
|  |  |  |  |  |

没有待确认项时写“无”，不要保留空表。

## 4. 后续实现依赖

| 优先级 | 逻辑对象 | 代码路径或符号 | 缺少的行为 | 后续阶段 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  | game-scaffold / game-implement | 待处理 |

没有代码依赖时写“无”。这里只记录入口、行为和风险，不在本阶段修改代码。

## 5. 执行与验证

- 已修改真实源：<文件、Sheet 或对象摘要；无则写“无”>
- Schema：未变化 / 已修改并验证 / 待确认
- 数据生成：通过 / 失败 / 未执行（原因）
- 类型或访问代码生成：通过 / 失败 / 不需要 / 未执行（原因）
- 校验覆盖：<类型/键/引用/范围/资源/跨记录中实际执行的类别>
- 输出目标：<客户端/服务端/工具的结果摘要>
- 加载或消费端：通过 / 失败 / 待代码接入
- 可重复生成：通过 / 未执行（原因） / 不适用
- 误改代码处置：无 / 已安全回退 / 待用户处理
- 剩余风险：无 / <最多 2 条>

> 完整 diff、单元格明细、生成物列表和日志保留在配置源、仓库或独立执行清单中，不复制到本文件。
