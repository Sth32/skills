# <功能名>：配置规划

> 状态：规划中 / 待确认范围 / 修改中 / 已完成
> 配置源：Excel / XML / YAML / JSON / 混合
> Excel：待确认 / 手动 / 自动 / 不涉及
> 最后更新：<日期或提交>

## 1. 配置结论

- 配置范围：<涉及多少个真实配置源>
- 新增表或 Schema：否 / 是（只写原因）
- 关键保留代码：<最多 3 条；没有则写“无”>
- 当前阻塞：<P0/P1 未决项；没有则写“无”>
- 后续代码依赖：<数量；没有则写“无”>

## 2. 配置目标与执行状态

> 一行对应一个目标文件、Sheet、表或配置实体。先分类，再决定是否修改。只写变化字段。

| 目标文件 / Sheet / 表 | 类别 | 格式 | 新增或修改对象 | 关键差异 | 执行方式 | 状态 |
|---|---|---|---|---|---|---|
|  | 配置源 / 生成物 / Schema 元数据 / 运行时代码 | Excel / XML / YAML / JSON / Lua 数据等 |  |  | 直接修改 / Excel 手动 / Excel 自动 / 不直改 / 后续代码 | 待确认 / 待配置 / 待人工 / 已完成 / 待代码接入 |

类别规则：

- **配置源**：声明式数据，由通用加载器或导表链路消费；本阶段可在确认后修改；
- **生成物**：由其他源生成；不直接修改；
- **Schema 元数据**：字段、类型或校验声明；单独确认；
- **运行时代码**：Component、Manager、Handler、`VIEW_MAP` 等；本阶段不修改。

## 3. 需要确认

| 优先级 | 目标源 | 需要决定的事项 | 推荐或默认处理 | 负责人 / 状态 |
|---|---|---|---|---|
|  |  |  |  |  |

没有待确认项时写“无”，不要保留空表。

## 4. 后续实现依赖

| 优先级 | 代码路径或符号 | 缺少的行为 | 关联配置 | 后续阶段 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  | game-scaffold / game-implement | 待处理 |

没有代码依赖时写“无”。这里只记录实现入口和影响，不在本阶段修改代码。

## 5. 执行与验证

- 已修改配置源：<文件与对象摘要；无则写“无”>
- Excel：手动待人工 / 自动已修改 / 不涉及
- 生成物：由标准流程生成 / 未生成（原因）
- 格式或 Schema 校验：通过 / 失败 / 未执行
- 导表或生成：通过 / 失败 / 未执行
- 加载或引用：通过 / 失败 / 待代码接入
- 误改代码处置：无 / 已安全回退 / 待用户处理
- 剩余风险：无 / <最多 2 条>

> 完整 diff、单元格明细和日志保留在配置源、代码仓库或独立执行清单中，不复制到本文件。XML、YAML、JSON 等声明式数据可在确认后修改；运行时代码无论扩展名为何，都只记录为后续依赖。