<!--
启动门禁：创建或继续本文档前，必须先读取 `01-原始需求.md`、`02-需求挖掘.md`、`03-程序实现澄清.md` 的顶部状态。正常进入本阶段要求 `01=整理完成`、`02=已收敛`、`03=已收敛`。任一状态缺失、异常、仍在进行或被阻塞时，必须先明确提醒用户并返回对应上游闭环；不得仅凭聊天中的“已完成”继续。
阶段完成门禁：只有满足本阶段完成条件、把顶部状态写为 `已完成`、同步未解决计数与最后更新时间，并重新读取确认写入成功后，才能宣布本阶段结束或建议进入 `game-scaffold`。聊天结论不能替代顶部状态。
顶部“状态”只表示配置阶段自身是否闭环；“后续代码依赖”必须留在对应配置事项内，不得用 `待代码接入` 作为阶段顶部状态。
-->

# <功能名>：配置规划

> 状态：规划中 / 待确认 / 修改中 / 待人工 / 已完成  
> Excel：手动 / 自动 / 不涉及  
> 未解决：P0 <N> / P1 <N> / P2 <N>  
> 最后更新：<日期或提交>

## 配置事项

### C-001 · <逻辑配置对象> · <状态>

- **真实源 / Schema**：<文件、Sheet、字段或声明式 Schema；不列生成物>
- **关键变化**：<参照对象 + 变化字段，不复制完整行或表结构>
- **执行方式**：<Agent 修改 / 人工填表 / 待确认>
- **当前结果**：<已修改内容，或仍需完成的最小操作>
- **验证**：<实际执行的解析、类型、引用、导表、输出或加载检查及结果>
- **后续代码依赖**：<缺少的行为、代码入口和后续阶段；没有则删除本行>

### C-002 · <下一个独立配置对象> · <状态>

<没有则删除>

<!--
每个逻辑配置对象只出现一次。真实源、变化、人工步骤、执行状态、验证结果和代码依赖必须相邻。
不得另建“配置执行清单”“人工填表清单”“验证报告”“调查记录”或“移交摘要”。
用户选择人工填表时，直接在对应事项的“当前结果”中写清 Sheet、定位方式、字段和校验方式。
执行或沟通完成后原位更新：删除已解决问题、旧步骤、旧状态和失效说明，只保留当前真相。
后续代码依赖属于移交信息：配置阶段自身已完成时，顶部状态应写 `已完成`，而不是 `待代码接入`。
完整 diff、生成日志和执行历史保留在仓库或工具输出中，不复制到本文档。
没有配置事项时不要创建空章节，也不要写“无”。
-->
