# <功能名>：策划配表说明

> 本文档是配置阶段收尾交付物，不记录阶段状态、技术调查过程或验证流水。
> “策划配表提醒”和“功能配表说明”用途不同，必须分开维护，不合并成一个大表。

## 策划配表提醒

- <文件 / Sheet / 配置对象>：<需要策划补什么；写清合法 ID 业务域、数值范围等必要约束，不要求程序阶段预填最终业务值>
- <下一条提醒；尽量一句话>

<!--
这里只放不阻塞当前实现、可由策划后续补齐的零碎配置。
优先写“在哪 / 配什么 / 合法 ID 域或必要范围约束”，不要写技术背景、执行历史和长解释。
例如奖励只要求“发放货币”时，应写清 reward_id 只能填货币类 ID、count 的必要范围；具体货币 ID 和数量由策划按实际需求填写。
如果最终取值会影响 Schema、查找/引用、加载、控制流、协议语义或测试成立条件，就不能放在这里拖后处理，必须回到 04-配置规划.md 收敛。
没有提醒时删除本节，不写“无”。
-->

## 功能配表说明

<用 1～3 句话说明这组配置共同控制什么，以及主要配置对象之间的关系。>

| 配置源 / 对象 | 主键或定位方式 | 主体字段 | 字段含义 / 代码用途 | 关键约束 |
|---|---|---|---|---|
| `<表 / Sheet / 配置对象>` | `<id / type / 组合键>` | `<field_a>` | <代码为什么读取它> | <合法 ID 业务域、引用、范围、枚举等真正重要的约束> |
|  |  | `<field_b>` | <主体字段含义> |  |

<!--
这里只介绍当前功能的重要配表，以及现有代码已消费或实现阶段已明确会直接读取的主体字段。
不要列完整 Schema，不要为了完整性加入纯导出/生成技术字段、显然沿用默认值的字段、策划编辑器内部辅助字段或零碎配表提醒。
同一配置对象有多个主体字段时可以续行；字段很多时按业务含义聚合，不做字段百科。
表格之外只保留理解配置关系所需的短说明。
没有值得说明的主体配置时删除本节；如果两节都为空，则不生成本文档。
-->
